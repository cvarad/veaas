import React from 'react';

const SubmitComponent = (props) => (
    <div>
        <button onClick={props.onSubmitClick}>Submit Video For Editing!</button>
    </div>
)

export default SubmitComponent;